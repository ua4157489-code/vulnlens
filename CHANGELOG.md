# Changelog

## [0.1.0]: Initial release
- Nmap XML and OpenVAS/Greenbone XML collectors
- Normalized `Finding` / `Asset` / `ScanResult` models
- CLI: `vulnlens parse` with format auto-detection, severity filter, JSON export
- Safe XML parsing with `defusedxml`
